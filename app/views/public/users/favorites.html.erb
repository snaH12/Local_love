<div class="container">
  <div class="row">
    <div class="col-9">
      <h3><strong>お気に入り投稿一覧</strong>（全<%= @favorite_posts.count %>件）</h3>
      <div class="row mt-3">
      <% if @favorite_posts.count != 0 %>
        <% @favorite_posts.each do |post|%>
        <div class="card col-2" style="margin:20px;">
          <diV class="card-img-top" style="margin:5px">
          <%= link_to post_path(post.id) do %>
            <%= image_tag post.get_image(100,100), class: "mb-2 d-block w-100", style: "background-size: cover; background-position: center;" %>
            <p class="mb-2"><%= post.title%></p>
          <% end %>
          </diV>
          <p class="mb-2"><%= post.region.name %></p>
          <div id=<%="favorite-box"+ post.id.to_s %>>
          <p class="mb-2"><%= render 'public/favorites/favorite', post: post %></p>
          </div>
        </div>
        <% end %>
      <% else %>
     　　<h2>お気に入りの投稿はありません。</h2>　
     <% end %>
   </div>
  </div>
</div>