<h1>コメント一覧</h1>
<div>
    <% @comments.each do |comment| %>
    <div>
        <p><%= comment.comment %></p>
        <p><%= comment.user.name %></p>
        <p><%= link_to "削除" , admin_comment_path(comment.id),method: :delete %></p>
    </div>
    <% end %>
</div>