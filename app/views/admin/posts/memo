 
<% @comments.each do |comment| %>
<table>
<tr>
<td><%= comment.user.name %> : <%= comment.comment %></td>
<td><%= link_to "削除" , admin_comment_path(comment.id),method: :delete %></td>
</tr>
</table>
<% end %>


<table class="table table-borderless">
  <tbody>
      <% post.comments.each do |comment| %>
      <tr>
        
        <td>
            <%= comment.user.name %> : <%= comment.comment %></td>
        <td>
            <%= link_to "コメント削除", post_comment_path(post, comment), method: :delete, remote: true, class: "btn btn-danger pull-right" %>
        </td>
      </tr>
      <% end %>
  </tbody>
</table>


class: "btn btn-danger pull-right"
<%= render "admin/comments/index", comments: @comments %>


    def destroy
       @comment = Comment.find(params[:id])
       @comment.destroy
       redirect_to admin_comments_path
    end